export default function ScooterDetail({ params }) {
    return (
        <>
            <h1 className="contPrincipal">Estoy en la pagina de detalle del producto {params.id}</h1>
        </>
        )
    }